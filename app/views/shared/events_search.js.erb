var result = "[data-search-target]";
$(result).html("<%= j render partial: partial, collection: collection, as: local %>");
/* same as .empty().append() */

/* something like this might be cool */
/* window.location.hash = '<%= "search" %>'; */