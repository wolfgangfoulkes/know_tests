<%= render partial: 'events/event', 
	locals: 
	{ 
		event: event,
		svg_thn: 'svg/test/arrow_3.svg',
		link: false,
		data: {}.merge( drop_snd(event.id) )
	} 
%>
<%= 
content_tag :div,
class: "activity-list col h4", 
data: drop_rcv(event.id) do
%>
  <%= render_activities(event.activities.page(1).per(10), layout: :activity) %>
<% end %>