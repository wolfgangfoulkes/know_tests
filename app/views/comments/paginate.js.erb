$("[data-scroll-target=<%= comments.type %>]").empty().append("<%= j render comments %>");
$("[data-pagi-link=<%= comments.type %>]").empty().append("<%= j paginate comments, remote: true, params: {controller: 'comments', action: 'paginate', role: comments.role} %>");